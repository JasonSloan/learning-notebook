{
    "version": "2.0.0", 
    "tasks": [
        {
            "label": "Build", 
            "type": "shell", 
            "command": "rm -rf ./** && cmake .. && make", 
            "options": {
                "cwd": "${workspaceFolder}/build" 
            }
        }
    ]
}